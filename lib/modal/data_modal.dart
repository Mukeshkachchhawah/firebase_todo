class DataModal {
  String title;
  String desc;
  // bool isDone;

  DataModal({required this.desc, required this.title});

  // factory DataModal.fromjson(Map<String, dynamic> json) {
  //   return DataModal(desc: json['desc'], title: json['title']);
  // }
}
